"""Engine layer — слой движков поиска стратегий (2026-09-05).

Идеология Апостола: комбайн = каркас (данные → бэктест → гейты → пул → forward).
Меняется только «технология изготовления стратегий». Каждый движок — отдельный
механизм ПОИСКА; все ссыпают кандидатов в ОДНУ воронку (state/engine_candidates.json),
которую дальше оценивает tools/select_stable_pool.py с теми же гейтами.

Движки:
  E1 zoo/autopilot  — готовые семейства стратегий × сетка параметров (существует).
  E2 genetic        — эволюционный поиск НОВЫХ стратегий из DSL-атомов (genetic_engine.py).
  E3+ ...           — добавлять новые файлы engines/<name>_engine.py с интерфейсом
                      run(ticker, df) -> [candidates].
"""

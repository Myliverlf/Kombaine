# Iteration 23M — Family Verdicts

- `atr_breakout` = `ROBUST_FAMILY`
- `atr_trailing_stop` = `ROBUST_FAMILY`

Both families are robust in the bounded sense, but materially redundant across parameter variants. The bounded forward set keeps one representative per family.

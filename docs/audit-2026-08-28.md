# Аудит комбайна — 2026-08-28

## Критические проблемы (3)
1. **pipeline_ranker НЕ в live-пути** — все фильтры (regime gate, risk scorecard, lifecycle, forecast) пропускаются. Supervisor использует inline-код.
2. **strategy_replacement_policy отключён** — portfolio_aware_score/adaptive_threshold мёртвы, supervisor сравнивает PnL only.
3. **Supervisor мутирует derived views которые не сохраняются** — expired candidates и pruning = no-ops.

## Серьёзные недочёты (5)
4. Два пути promotion (legacy dict vs canonical registry)
5. go_rub теряется при миграции (все signal_pool = 0.0)
6. Два rank_score на стратегию (active_rank vs metrics.rank_score)
7. Deferred eject может deadlock
8. generator_feedback table — мёртвая схема

## Архитектурные недочёты (4)
9. Три реализации profit_factor
10. strategy_registry.json растёт бесконечно (18MB, per-strategy history unbounded)
11. Нет mid-position regime monitor
12. Нет strategy-family diversity enforcement в live

## Что работает хорошо
- Eject rules R1-R4
- Reconciliation с брокером
- Delta band control
- Portfolio stop
- Lock file protection

## Топ-7 приоритетов (ВЫПОЛНЕНО 2026-08-28)
1. ✅ Встроить pipeline_ranker в supervisor — regime gate pre-filter
2. ✅ Встроить replacement_policy в promotion — portfolio_aware_score
3. ✅ Фикс mutations → registry — expired candidates и pruning теперь работают
4. ✅ Один profit_factor — удалён мёртвый _slot_profit_factor
5. ✅ Prune per-strategy history — max 50 events/record
6. ✅ Eject timeout — 48ч deadlock protection
7. ✅ Mid-position regime check — log warnings only
